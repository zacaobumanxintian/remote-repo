中缀表达式转化为后缀表达式（也称为逆波兰表达式）可以通过 "逆波兰记法" 算法实现，通常使用栈来辅助操作。这种方法也称为 "Shunting Yard" 算法，由 Edsger Dijkstra 发明。

### 转换规则

1. **操作数**：直接输出。
2. **左括号 `(`**：压入栈。
3. **右括号 `)`**：弹出栈顶运算符并输出，直到遇到左括号 `(`，将左括号丢弃。
4. **运算符**：弹出所有优先级大于或等于当前运算符的栈顶运算符，并输出。然后将当前运算符压入栈。

### 